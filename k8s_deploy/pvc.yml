apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-go
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # volumeMode: Filesystem
